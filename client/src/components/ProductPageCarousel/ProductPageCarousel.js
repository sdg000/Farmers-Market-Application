import './ProductPageCarousel.css'

function ProductPageCarousel(){

    return(

        <div class='pic-ctn'>
            {/* {allProducts?allProducts.map((product) => {
                return (
                    <img id={product.id} class='pic' src={product.image_url} alt={product.name}/>
                )

            }):null} */}

            <p>carousel page</p>


        </div>
    )

}
export default ProductPageCarousel